import App, { RootErrorBoundary, RootLinks, RootLayout } from "@CS/web/routes/Root";

export const links = RootLinks;
export const Layout = RootLayout;
export default App;
export const ErrorBoundary = RootErrorBoundary;
